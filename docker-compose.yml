services:
    chroma:
        image: chromadb/chroma:latest
        container_name: chroma_thanos
        ports:
            - "8000:8000"
        volumes:
            - chroma-data:/chroma/chroma
        environment:
            - IS_PERSISTENT=TRUE
            - ANONYMIZED_TELEMETRY=FALSE

volumes:
    chroma-data:
